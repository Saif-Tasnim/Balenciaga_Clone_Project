import React from 'react';
import SearchBox from '../../Components/SearchBox/SearchBox';

const HomePage = () => {
    return (
        <div>
           <SearchBox></SearchBox>
        </div>
    );
};

export default HomePage;